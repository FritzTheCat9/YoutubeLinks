<MudDialog>
    <DialogContent>
        <MudText>
            @ContentText
        </MudText>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel">
            @Localizer[nameof(AppStrings.Cancel)]
        </MudButton>
        <MudButton Color="Color"
                   Variant="Variant.Filled"
                   OnClick="Submit"
                   UserAttributes="@(new Dictionary<string, object> { { TestConst.TestId, InformationDialogConst.ConfirmButton } })">
            @ButtonText
        </MudButton>
    </DialogActions>
</MudDialog>
